<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pimpledmikey.nexus_goll_final">

    <application
        android:label="Nexus MK"
        android:icon="@mipmap/ic_launcher">

        <!-- Nota: Se evita marcar la app como herramienta de monitorización.
                 Google Play puede interpretar valores como "enterprise_management"
                 como indicio de funcionalidades de monitoring. Para reducir riesgo
                 de rechazo, este valor se deja en "false" y se documenta la razón
                 en el paquete de revisión. Si la app es una solución EMM/enterprise,
                 indicarlo explícitamente en Play Console y usar Managed Google Play. -->
        <meta-data
            android:name="isMonitoringTool"
            android:value="enterprise_management" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">

            <meta-data android:name="io.flutter.embedding.android.NormalTheme" android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
